[impala]
HOST = 192.168.1.112
PORT = 21000
DATABASE = gameadmindw

[request]
HEADERS = the_date|role_cnt|cnt
QUERY = """
SELECT
    TO_DATE(`the_date`) AS the_date,
    count(distinct unique_chr_id) AS role_cnt,
    count(*) AS cnt
FROM `raw_logins`
GROUP BY the_date
ORDER BY the_date DESC
"""